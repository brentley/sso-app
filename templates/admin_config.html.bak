{% extends "base.html" %}

{% block title %}SSO Authentication Test - Configuration{% endblock %}

{% block content %}
<div class="row">
    <div class="col-12">
        <div class="d-flex justify-content-between align-items-center mb-4">
            <h2>Configuration Settings</h2>
            <a href="{{ url_for('admin') }}" class="btn btn-secondary">Back to Dashboard</a>
        </div>

        <!-- SAML Configuration -->
        <form id="samlForm">
            <div class="card mb-4">
                <div class="card-header">
                    <h5 class="mb-0">SAML 2.0 Configuration</h5>
                </div>
                <div class="card-body">
                    <!-- Service Provider Information -->
                    <div class="alert alert-info mb-4">
                        <h6 class="alert-heading"><strong>📋 Service Provider Information</strong></h6>
                        <p class="mb-2"><strong>Use these values when configuring this app in Authentik:</strong></p>
                        <div class="row">
                            <div class="col-md-6">
                                <strong>Entity ID (SP Entity ID):</strong><br>
                                <code class="fs-6 bg-white text-dark p-2 d-block border rounded">{{ request.url_root.rstrip('/') }}</code>
                            </div>
                            <div class="col-md-6">
                                <strong>ACS URL (Assertion Consumer Service):</strong><br>
                                <code class="fs-6 bg-white text-dark p-2 d-block border rounded">{{ request.url_root.rstrip('/') }}/saml/acs</code>
                            </div>
                        </div>
                        <div class="row mt-2">
                            <div class="col-md-6">
                                <strong>SLS URL (Single Logout Service):</strong><br>
                                <code class="fs-6 bg-white text-dark p-2 d-block border rounded">{{ request.url_root.rstrip('/') }}/saml/sls</code>
                            </div>
                            <div class="col-md-6">
                                <strong>NameID Format:</strong><br>
                                <code class="fs-6 bg-white text-dark p-2 d-block border rounded">urn:oasis:names:tc:SAML:1.1:nameid-format:emailAddress</code>
                            </div>
                        </div>
                    </div>

                    <!-- Metadata Import -->
                    <div class="alert alert-primary mb-4">
                        <h6 class="alert-heading"><strong>📥 Import from Authentik Metadata</strong></h6>
                        <p class="mb-3">Automatically configure SAML settings by importing from Authentik metadata URL:</p>
                        <div class="row">
                            <div class="col-md-8 mb-2">
                                <input type="url" class="form-control" id="saml_metadata_url" name="saml_metadata_url"
                                       placeholder="https://auth.visiquate.com/api/v3/providers/saml/[id]/metadata/">
                                <div class="form-text">From Authentik: Applications → [Your App] → Provider → Metadata URL</div>
                            </div>
                            <div class="col-md-4 mb-2">
                                <button type="button" class="btn btn-primary w-100" onclick="importSamlMetadata()">
                                    <i class="fas fa-download me-2"></i>Import Metadata
                                </button>
                            </div>
                        </div>
                    </div>

                    <!-- Identity Provider Configuration -->
                    <h6 class="mb-3"><strong>🔗 Identity Provider Settings</strong></h6>
                    <p class="text-muted mb-3">Enter the values from your Authentik SAML provider configuration or import from metadata above:</p>
                    
                    <div class="row">
                        <div class="col-md-6 mb-3">
                            <label for="saml_idp_entity_id" class="form-label"><strong>Authentik Entity ID</strong></label>
                            <input type="text" class="form-control" id="saml_idp_entity_id" name="saml_idp_entity_id" 
                                   value="{{ configs.get('saml_idp_entity_id').value if configs.get('saml_idp_entity_id') else '' }}"
                                   placeholder="https://auth.visiquate.com/api/v3/providers/saml/[id]/metadata/">
                            <div class="form-text">From Authentik: Applications → [Your App] → Provider → Issuer</div>
                        </div>
                        <div class="col-md-6 mb-3">
                            <label for="saml_idp_sso_url" class="form-label"><strong>Authentik SSO URL</strong></label>
                            <input type="url" class="form-control" id="saml_idp_sso_url" name="saml_idp_sso_url"
                                   value="{{ configs.get('saml_idp_sso_url').value if configs.get('saml_idp_sso_url') else '' }}"
                                   placeholder="https://auth.visiquate.com/api/v3/providers/saml/[id]/sso/binding/redirect/">
                            <div class="form-text">From Authentik: Applications → [Your App] → Provider → SSO URL</div>
                        </div>
                    </div>
                    <div class="row">
                        <div class="col-md-6 mb-3">
                            <label for="saml_idp_slo_url" class="form-label">Authentik SLO URL (Optional)</label>
                            <input type="url" class="form-control" id="saml_idp_slo_url" name="saml_idp_slo_url"
                                   value="{{ configs.get('saml_idp_slo_url').value if configs.get('saml_idp_slo_url') else '' }}"
                                   placeholder="https://auth.visiquate.com/api/v3/providers/saml/[id]/slo/binding/redirect/">
                            <div class="form-text">From Authentik: Applications → [Your App] → Provider → SLO URL</div>
                        </div>
                        <div class="col-md-6 mb-3">
                            <label for="saml_nameid_format" class="form-label">NameID Format</label>
                            <select class="form-select" id="saml_nameid_format" name="saml_nameid_format">
                                <option value="urn:oasis:names:tc:SAML:1.1:nameid-format:emailAddress" 
                                    {{ 'selected' if configs.get('saml_nameid_format') and configs.get('saml_nameid_format').value == 'urn:oasis:names:tc:SAML:1.1:nameid-format:emailAddress' else 'selected' }}>
                                    Email Address (Recommended)
                                </option>
                                <option value="urn:oasis:names:tc:SAML:2.0:nameid-format:persistent"
                                    {{ 'selected' if configs.get('saml_nameid_format') and configs.get('saml_nameid_format').value == 'urn:oasis:names:tc:SAML:2.0:nameid-format:persistent' else '' }}>
                                    Persistent
                                </option>
                            </select>
                        </div>
                    </div>
                    <div class="mb-3">
                        <label for="saml_idp_cert" class="form-label"><strong>Authentik Signing Certificate</strong></label>
                        <textarea class="form-control" id="saml_idp_cert" name="saml_idp_cert" rows="8" 
                                  placeholder="-----BEGIN CERTIFICATE-----&#10;MIICXjCCAUYCCQ...&#10;-----END CERTIFICATE-----">{{ configs.get('saml_idp_cert').value if configs.get('saml_idp_cert') else '' }}</textarea>
                        <div class="form-text">From Authentik: Applications → [Your App] → Provider → Download Certificate. Include the BEGIN/END lines.</div>
                    </div>
                    <div class="row">
                        <div class="col-md-6 mb-3">
                            <label for="saml_sp_cert" class="form-label">SP X.509 Certificate (Optional)</label>
                            <textarea class="form-control" id="saml_sp_cert" name="saml_sp_cert" rows="4"
                                      placeholder="-----BEGIN CERTIFICATE-----&#10;...&#10;-----END CERTIFICATE-----">{{ configs.get('saml_sp_cert').value if configs.get('saml_sp_cert') else '' }}</textarea>
                            <div class="form-text">Service Provider certificate for signed requests</div>
                        </div>
                        <div class="col-md-6 mb-3">
                            <label for="saml_sp_private_key" class="form-label">SP Private Key (Optional)</label>
                            <textarea class="form-control" id="saml_sp_private_key" name="saml_sp_private_key" rows="4"
                                      placeholder="-----BEGIN PRIVATE KEY-----&#10;...&#10;-----END PRIVATE KEY-----">{{ configs.get('saml_sp_private_key').value if configs.get('saml_sp_private_key') else '' }}</textarea>
                            <div class="form-text">Private key for SP certificate</div>
                        </div>
                    </div>
                    
                    <div class="text-end">
                        <button type="submit" class="btn btn-primary">
                            <i class="fas fa-save me-2"></i>Save SAML Configuration
                        </button>
                    </div>
                </div>
            </div>
        </form>

        <!-- OIDC Configuration -->
        <form id="oidcForm">
            <div class="card mb-4">
                <div class="card-header">
                    <h5 class="mb-0">OpenID Connect (OIDC) Configuration</h5>
                </div>
                <div class="card-body">
                    <!-- OIDC Redirect URI Information -->
                    <div class="alert alert-info mb-4">
                        <h6 class="alert-heading"><strong>🔗 OIDC Configuration Info</strong></h6>
                        <p class="mb-2"><strong>Use this redirect URI when configuring the OIDC provider in Authentik:</strong></p>
                        <div class="row">
                            <div class="col-md-8">
                                <strong>Redirect URI:</strong><br>
                                <code class="fs-6 bg-white text-dark p-2 d-block border rounded">{{ request.url_root.rstrip('/') }}/oauth/callback/authentik</code>
                            </div>
                            <div class="col-md-4">
                                <strong>Scopes:</strong><br>
                                <code class="fs-6 bg-white text-dark p-2 d-block border rounded">openid email profile</code>
                            </div>
                        </div>
                    </div>

                    <!-- OIDC Discovery Import -->
                    <div class="alert alert-primary mb-4">
                        <h6 class="alert-heading"><strong>📥 Import from OIDC Discovery</strong></h6>
                        <p class="mb-3">Automatically configure OIDC settings by importing from Authentik discovery URL:</p>
                        <div class="row">
                            <div class="col-md-8 mb-2">
                                <input type="url" class="form-control" id="oidc_discovery_url" name="oidc_discovery_url"
                                       value="{{ configs.get('oidc_discovery_url').value if configs.get('oidc_discovery_url') else '' }}"
                                       placeholder="https://id.visiquate.com/application/o/[slug]/.well-known/openid_configuration">
                                <div class="form-text">From Authentik: Applications → [Your App] → Provider → OpenID Configuration URL</div>
                            </div>
                            <div class="col-md-4 mb-2">
                                <button type="button" class="btn btn-primary w-100" onclick="importOidcDiscovery()">
                                    <i class="fas fa-download me-2"></i>Import Discovery
                                </button>
                            </div>
                        </div>
                    </div>

                    <!-- Authentik OIDC Settings -->
                    <h6 class="mb-3"><strong>🔐 Authentik OIDC Provider Settings</strong></h6>
                    <p class="text-muted mb-3">Configure your Authentik OIDC provider credentials or import from discovery above:</p>
                    
                    <div class="row">
                        <div class="col-md-6 mb-3">
                            <label for="oidc_authentik_url" class="form-label"><strong>Authentik Server URL</strong></label>
                            <input type="url" class="form-control" id="oidc_authentik_url" name="oidc_authentik_url"
                                   value="{{ configs.get('oidc_authentik_url').value if configs.get('oidc_authentik_url') else '' }}"
                                   placeholder="https://auth.visiquate.com">
                            <div class="form-text">Your Authentik instance base URL (without trailing slash)</div>
                        </div>
                        <div class="col-md-6 mb-3">
                            <label for="oidc_authentik_client_id" class="form-label"><strong>Client ID</strong></label>
                            <input type="text" class="form-control" id="oidc_authentik_client_id" name="oidc_authentik_client_id"
                                   value="{{ configs.get('oidc_authentik_client_id').value if configs.get('oidc_authentik_client_id') else '' }}"
                                   placeholder="Generated by Authentik">
                            <div class="form-text">From Authentik: Applications → [Your App] → Provider → Client ID</div>
                        </div>
                    </div>
                    <div class="row">
                        <div class="col-md-6 mb-3">
                            <label for="oidc_authentik_client_secret" class="form-label"><strong>Client Secret</strong></label>
                            <input type="password" class="form-control" id="oidc_authentik_client_secret" name="oidc_authentik_client_secret"
                                   value="{{ configs.get('oidc_authentik_client_secret').value if configs.get('oidc_authentik_client_secret') else '' }}"
                                   placeholder="Generated by Authentik">
                            <div class="form-text">From Authentik: Applications → [Your App] → Provider → Client Secret</div>
                        </div>
                        <div class="col-md-6 mb-3">
                            <label class="form-label"><strong>Provider Type</strong></label>
                            <select class="form-select" disabled>
                                <option selected>OAuth2/OpenID Provider</option>
                            </select>
                            <div class="form-text">Use OAuth2/OpenID Provider type in Authentik</div>
                        </div>
                    </div>
                    
                    <div class="text-end">
                        <button type="submit" class="btn btn-primary">
                            <i class="fas fa-save me-2"></i>Save OIDC Configuration
                        </button>
                    </div>
                </div>
            </div>
        </form>

        <!-- SCIM Configuration -->
        <form id="scimForm">
            <div class="card mb-4">
                <div class="card-header">
                    <h5 class="mb-0">SCIM Configuration</h5>
                </div>
                <div class="card-body">
                    <div class="row">
                        <div class="col-md-6 mb-3">
                            <div class="form-check form-switch">
                                <input class="form-check-input" type="checkbox" id="scim_enabled" name="scim_enabled"
                                       {{ 'checked' if configs.get('scim_enabled') and configs.get('scim_enabled').value == 'true' else '' }}>
                                <label class="form-check-label" for="scim_enabled">Enable SCIM Provisioning</label>
                            </div>
                            <div class="form-text">Allow Authentik to provision users automatically</div>
                        </div>
                        <div class="col-md-6 mb-3">
                            <label for="scim_bearer_token" class="form-label">SCIM Bearer Token</label>
                            <div class="input-group">
                                <input type="password" class="form-control" id="scim_bearer_token" name="scim_bearer_token"
                                       value="{{ configs.get('scim_bearer_token').value if configs.get('scim_bearer_token') else '' }}">
                                <button class="btn btn-outline-secondary" type="button" onclick="generateScimToken()">Generate</button>
                            </div>
                            <div class="form-text">Used to authenticate SCIM requests from Authentik</div>
                        </div>
                    </div>
                    <div class="alert alert-info">
                        <strong>SCIM Endpoint:</strong> <code id="scim-endpoint">{{ request.url_root }}scim/v2/Users</code><br>
                        <strong>Service Provider Config:</strong> <code>{{ request.url_root }}scim/v2/ServiceProviderConfig</code>
                    </div>
                    
                    <div class="text-end">
                        <button type="submit" class="btn btn-primary">
                            <i class="fas fa-save me-2"></i>Save SCIM Configuration
                        </button>
                    </div>
                </div>
            </div>
        </form>

        <!-- Application Settings -->
        <form id="appForm">
            <div class="card mb-4">
                <div class="card-header">
                    <h5 class="mb-0">Application Settings</h5>
                </div>
                <div class="card-body">
                    <div class="row">
                        <div class="col-md-6 mb-3">
                            <label for="app_origin" class="form-label">Application Origin</label>
                            <input type="url" class="form-control" id="app_origin" name="app_origin"
                                   value="{{ configs.get('app_origin').value if configs.get('app_origin') else request.url_root.rstrip('/') }}"
                                   placeholder="https://sso-test.example.com">
                            <div class="form-text">The base URL for this application</div>
                        </div>
                        <div class="col-md-6 mb-3">
                            <label for="session_timeout" class="form-label">Session Timeout (minutes)</label>
                            <input type="number" class="form-control" id="session_timeout" name="session_timeout"
                                   value="{{ configs.get('session_timeout').value if configs.get('session_timeout') else '60' }}"
                                   min="5" max="1440">
                        </div>
                    </div>
                    
                    <div class="text-end">
                        <button type="submit" class="btn btn-primary">
                            <i class="fas fa-save me-2"></i>Save Application Settings
                        </button>
                    </div>
                </div>
            </div>
        </form>
    </div>
</div>

<!-- Success Modal -->
<div class="modal fade" id="successModal" tabindex="-1">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title">Configuration Saved</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
            </div>
            <div class="modal-body">
                <p>Configuration has been successfully saved. Changes will take effect immediately.</p>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-primary" data-bs-dismiss="modal">OK</button>
            </div>
        </div>
    </div>
</div>
{% endblock %}

{% block extra_scripts %}
<script src="{{ url_for('static', filename='js/admin-config.js') }}?v={{ git_commit[:7] if git_commit else 'dev' }}" data-cf-async="false"></script>
{% endblock %}